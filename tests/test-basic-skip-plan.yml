name: Test basic skip-plan usage
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install Terraform
        run: |
          TERRAFORM_VERSION="1.9.8"
          curl -fsSL "https://releases.hashicorp.com/terraform/${TERRAFORM_VERSION}/terraform_${TERRAFORM_VERSION}_linux_amd64.zip" -o /tmp/terraform.zip
          unzip -q /tmp/terraform.zip -d /tmp/terraform-bin
          sudo mv /tmp/terraform-bin/terraform /usr/local/bin/terraform
          terraform version

      - id: action
        uses: .
        with:
          directory: '${{ github.workspace }}/tests/fixtures/terraform'
          comment: 'false'
          skip-plan: 'true'
          skip-fmt: 'true'
          skip-conftest: 'true'
          enable-drift-output: 'true'

      - name: Assert drift-output is set
        env:
          DRIFT: ${{ steps.action.outputs.drift-output }}
        run: |
          echo "drift-output: $DRIFT"
          if [ -z "$DRIFT" ]; then
            echo "FAIL: drift-output is empty"
            exit 1
          fi
          echo "PASS: drift-output is set"

      - name: Assert drift-output status is no_changes
        env:
          DRIFT: ${{ steps.action.outputs.drift-output }}
        run: |
          echo "drift-output: $DRIFT"
          STATUS=$(echo "$DRIFT" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['status'])")
          echo "status: $STATUS"
          if [ "$STATUS" != "no_changes" ]; then
            echo "FAIL: expected status 'no_changes', got '$STATUS'"
            exit 1
          fi
          echo "PASS: status is no_changes"

      - name: Assert drift-output hasChanges is false
        env:
          DRIFT: ${{ steps.action.outputs.drift-output }}
        run: |
          echo "drift-output: $DRIFT"
          HAS_CHANGES=$(echo "$DRIFT" | python3 -c "import sys,json; d=json.load(sys.stdin); print(str(d['hasChanges']).lower())")
          echo "hasChanges: $HAS_CHANGES"
          if [ "$HAS_CHANGES" != "false" ]; then
            echo "FAIL: expected hasChanges 'false', got '$HAS_CHANGES'"
            exit 1
          fi
          echo "PASS: hasChanges is false"

      - name: Assert drift-output directory is set
        env:
          DRIFT: ${{ steps.action.outputs.drift-output }}
        run: |
          echo "drift-output: $DRIFT"
          DIR=$(echo "$DRIFT" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['directory'])")
          echo "directory: $DIR"
          if [ -z "$DIR" ]; then
            echo "FAIL: expected directory to be set"
            exit 1
          fi
          echo "PASS: directory is set to '$DIR'"
